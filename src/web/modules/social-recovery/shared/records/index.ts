/**
 * The wallet's records. This folder holds no React component and no hook: a
 * hook belongs to the screen that needs it.
 */
export * from './types'
export * from './records'
export { extensionRecordStorage } from './extensionStorage'
